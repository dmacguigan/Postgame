name: sleeper-recap
channels:
  - conda-forge
dependencies:
  - python>=3.11
  - pip
  - pip:
      - requests
      - pytest
      # Install only the SDK for your chosen LLM provider:
      # - anthropic
      # - openai
      # - google-genai
